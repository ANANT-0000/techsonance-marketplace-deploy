export enum AdminErrorKeyEnum {
  INVALID_PASSWORD = 'Invalid password',
  FAILED_TO_LOGIN = 'Failed to login admin',
  FAILED_TO_FETCH_TOP_VENDORS = 'Failed to fetch top vendors',
  EMAIL_AND_PASSWORD_ARE_REQUIRED = 'Email and password are required',
  ADMIN_USER_NOT_FOUND = 'Admin user not found',
}
