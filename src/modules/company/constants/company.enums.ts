export enum CompanyOperationEnum {
  ACTIVATE = 'activate',
  DEACTIVATE = 'deactivate',
  SUSPEND = 'suspend',
}

export enum CompanyOperationResultEnum {
  ACTIVATED = 'activated',
  DEACTIVATED = 'deactivated',
  SUSPENDED = 'suspended',
}

export enum CompanyEnvironmentEnum {
  PRODUCTION = 'production',
}
